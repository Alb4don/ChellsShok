# ChellsShok
A scanner that identifies vulnerable endpoints by injecting diagnostic payloads into HTTP headers, URL parameters, and POST bodies, then detecting command execution via a unique response marker.
